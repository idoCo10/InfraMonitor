[project]
name = "inframonitor"
version = "0.1.0"
description = "Infrastructure monitoring and security visibility tool"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[tool.pytest.ini_options]
testpaths = ["tests"]